import { CopymeDirective } from './copyme.directive';

describe('CopymeDirective', () => {
  it('should create an instance', () => {
    const directive = new CopymeDirective();
    expect(directive).toBeTruthy();
  });
});
